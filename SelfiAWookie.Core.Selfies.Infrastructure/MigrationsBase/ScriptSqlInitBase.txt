--le faire dans le CMD

--1 - installer dotnet ef sur le Pc : dotnet tool install --global dotnet-ef	/* attention à la version installer sur le Pc */
--2 - créer la classe *DbContextFactory qui va être appelé lors de l'utilisation de la commande de la création des fichiers de Migration
--3 - lancer la commande de migration : dotnet ef migrations add NomdelaMigration --output-dir RépertoireEnregistrementDesFichiers
--4 - création du script sql : dotnet ef migrations script ou dotnet ef migrations script NomdelaMigration
--5 - exécuter la migration avec cette commande : dotnet ef database update

CREATE TABLE IF NOT EXISTS "__EFMigrationsHistory" (
    "MigrationId" character varying(150) NOT NULL,
    "ProductVersion" character varying(32) NOT NULL,
    CONSTRAINT "PK___EFMigrationsHistory" PRIMARY KEY ("MigrationId")
);

START TRANSACTION;
CREATE TABLE "Wookie" (
    "Id" integer GENERATED BY DEFAULT AS IDENTITY,
    "Name" character varying(100) NOT NULL,
    CONSTRAINT "PK_Wookie" PRIMARY KEY ("Id")
);

CREATE TABLE "Selfie" (
    "Id" integer GENERATED BY DEFAULT AS IDENTITY,
    "Title" text NOT NULL,
    "ImagePath" character varying(250),
    "WookieId" integer NOT NULL,
    CONSTRAINT "PK_Selfie" PRIMARY KEY ("Id"),
    CONSTRAINT "FK_Selfie_Wookie_WookieId" FOREIGN KEY ("WookieId") REFERENCES "Wookie" ("Id") ON DELETE CASCADE
);

CREATE INDEX "IX_Selfie_WookieId" ON "Selfie" ("WookieId");

INSERT INTO "__EFMigrationsHistory" ("MigrationId", "ProductVersion")
VALUES ('20260219085814_InitSelfie-Dev-SGBD', '10.0.3');

COMMIT;